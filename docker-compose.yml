services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dorm
      POSTGRES_PASSWORD: dorm
      POSTGRES_DB: dorm
      TZ: Asia/Bangkok
    ports:
      - "5432:5432"
    volumes:
      - dorm-db:/var/lib/postgresql/data

volumes:
  dorm-db:
